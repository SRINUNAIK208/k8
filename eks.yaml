apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: roboshop
  region: ap-south-1

managedNodeGroups:
 - name: roboshop
   instanceTypes: ["M7i-flex.large"]
   desiredCapacity: 3
   spot: true
   